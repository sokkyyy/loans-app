import { MoveDownDirective } from './move-down.directive';

describe('MoveDownDirective', () => {
  it('should create an instance', () => {
    const directive = new MoveDownDirective();
    expect(directive).toBeTruthy();
  });
});
